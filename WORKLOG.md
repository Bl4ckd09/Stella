# Worklog

2026-08-29 12:56  Vercel prod live on sunshine/stella as https://stella-ashy-seven.vercel.app (dpl_9sCtYVVQnTqiKk4LeESiaW4zZB69). Old sunshine123 project still serves https://stella-sandy-two.vercel.app but this CLI (sun559064) cannot manage it. 18 prod env vars uploaded from .env.local plus new HQ_PASSWORD and CRON_SECRET. Scanner verified: Tesco UB10 0NX → 4 properties → £3,476/yr SBRR.
2026-07-30 15:22  .env.local.example, README.md, layout.tsx, db.ts  |  Next: say `add stella agents` for the two bullets now, or `plan mcp` and I spec the MCP server first.
2026-07-30 15:28  .env.local.example, README.md, layout.tsx, db.ts  |  Next: say `resume build` and I start the MCP server, the `.claude/skills/` entry and the `claude -p` script on a worktree branch off `autonomous-agents`.
2026-07-30 15:28  .env.local.example, README.md, layout.tsx, db.ts  |  Next: say `resume build` and I start the MCP server, the `.claude/skills/` entry and the `claude -p` script on a worktree branch off `autonomous-agents`.
2026-09-08 18:39  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && git checkout -b wip/mistral-browser-voice && git add -A && git commit -m "WIP: browser Mistral voice widget"`
2026-09-08 19:09  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && git checkout -b wip/mistral-browser-voice && git add -A -- ':!data' && git commit -m "WIP: browser Mistral voice widget"`
2026-09-08 19:12  .env.local.example, .gitignore, README.md, layout.tsx  |  Next:
2026-09-08 22:45  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `git -C ~/stella status --porcelain | wc -l` must print 0 before any checkout; it prints 11 now, so run the WIP commit first:
2026-09-09 00:26  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && git checkout -b wip/mistral-browser-voice && git add -A -- ':!data' && git commit -m "WIP: browser Mistral voice widget"`
2026-09-09 00:31  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `mv ~/stella-pre ~/access-health`
2026-09-09 00:39  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && cp .env.local ~/stella-env.local.bak && git checkout -b wip/mistral-browser-voice && git add -A -- ':!data' && git commit -m "WIP: browser Mistral voice widget" && git push -u origin wip/mistral-browser-voice`
2026-09-09 01:04  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && cp .env.local ~/stella-env.local.bak && chmod 600 ~/stella-env.local.bak`
2026-09-09 01:25  .env.local.example, .gitignore, README.md, layout.tsx  |  Next: `cd ~/stella && cp .env.local ~/stella-env.local.bak && chmod 600 ~/stella-env.local.bak && git checkout -b wip/mistral-browser-voice && git add -A && git commit -m "WIP: browser Mistral voice widget" && git push -u origin wip/mistral-browser-voice`
